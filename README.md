# OpenClaw (cpak)

## Installation

```bash
cpak install github.com/containerpak/openclaw
```

## Usage

Start OpenClaw from the application menu or run:

```bash
cpak run github.com/containerpak/openclaw openclaw-desktop
```

The command line interface is also exported as `openclaw`.
